<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.ParkMyCar" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@color/purple_primary</item>

    </style>

    <style name="Theme.App.SplashScreenTheme" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/purple_primary</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/bg_splash</item>
        <item name="postSplashScreenTheme">@style/Theme.ParkMyCar</item>
    </style>
</resources>